collection_id: 106
collection_position: null
database_id: 9
dataset_query:
  database: 9
  native:
    query: |2-

      SELECT DATE_FORMAT(cs.left_at, '%Y-%m') AS leave_month, COUNT(*)
      FROM segment s
      JOIN contact_segment cs ON cs.segment_id = s.id AND cs.left_at IS NOT NULL
      LEFT JOIN consent c ON c.contact_id = cs.contact_id AND ABS(TIMESTAMPDIFF(MINUTE, c.created_at, cs.left_at)) <= 60 AND c.status = 'cancelled'
      WHERE s.name='Member' AND c.id IS NULL
      GROUP BY leave_month
  type: native
description: By month
display: line
id: 516
model: card
name: Count of members leaves without a cancelled consent over time
query_type: native
result_metadata:
- base_type: type/Text
  display_name: leave_month
  fingerprint:
    global:
      distinct-count: 52
      nil%: 0.0
    type:
      type/Text:
        average-length: 7.0
        percent-email: 0.0
        percent-json: 0.0
        percent-url: 0.0
  name: leave_month
  special_type: null
- base_type: type/BigInteger
  display_name: COUNT(*)
  fingerprint:
    global:
      distinct-count: 49
      nil%: 0.0
    type:
      type/Number:
        avg: 656.1346153846154
        max: 13407.0
        min: 2.0
        q1: 155.5
        q3: 582.5
        sd: 1855.5997395149861
  name: COUNT(*)
  special_type: null
visualization_settings:
  graph.dimensions:
  - leave_month
  graph.metrics:
  - COUNT(*)
  graph.x_axis.title_text: Leave month
  series_settings:
    COUNT(*):
      line.interpolate: step-after
      line.marker_enabled: false
      title: Count
