# CiviCRM loader

This directory contains SQL and Python loaders for different Keanu tables.

Best to see the order of enabled scripts with `keanu load -n`. 

## Caveats

- For donations, it relies on currency data created by currency_civicrm loader (see `../civicrm_currency`) 
